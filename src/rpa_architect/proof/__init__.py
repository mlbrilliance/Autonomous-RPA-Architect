"""Proof-of-execution video recording for the lifecycle agent.

This subpackage produces side-by-side MP4 demos of a UiPath robot run:
one stream showing the Orchestrator job state transitions, and one
stream showing the actual UI activity on the target web app
(Playwright re-run mode).
"""
