# {{ process_name }} — Migrated to Python + Playwright

Generated by the **Autonomous RPA Architect** XAML → Python migrator from a
UiPath REFramework dispatcher. The original `Main.xaml` / `Process.xaml`
are preserved in the audit trail.

## Run

```bash
pip install -e .[dev]
playwright install chromium
python main.py
```

## Behavior-parity tests

```bash
TARGET_URL=https://your-staging-app pytest
```

Each test asserts that every selector referenced in the source XAML still
resolves on the target page. If a selector drifted, the matching test fails
with the original target-display-name so the engineer can update the flow.

## Transactions

{% for tx in transactions -%}
- `processes.{{ tx.module }}.{{ tx.fn_name }}` — {{ tx.name }}
{% endfor %}
